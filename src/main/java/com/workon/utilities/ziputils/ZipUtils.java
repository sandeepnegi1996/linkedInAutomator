package com.workon.utilities.ziputils;

public class ZipUtils {

	public static void zipReportFolder() {
		/*
		 * String reportPath = Page.rootDir + "\\target\\surefire-reports\\html"; String
		 * OUTPUT_ZIP_FILE = Page.rootDir+"\\ZIP\\reports.zip";
		 * 
		 * String SOURCE_FOLDER = reportPath; // SourceFolder paths
		 * 
		 * try { ZipUtil.pack(new File(SOURCE_FOLDER), new File(OUTPUT_ZIP_FILE));
		 * }catch(Exception e) {
		 * Log.info("Exception occured inside the zip utils class");
		 * e.printStackTrace(); }
		 */
	}

}
